<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_school_student_kanban" model="ir.ui.view">
        <field name="name">school.student.kanban</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <kanban default_group_by="class_id">
                <field name="name"/>
                <field name="student_code"/>
                <field name="image"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="status"/>
                <field name="class_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card">
                            <div class="oe_kanban_content">
                                <div class="oe_kanban_image">
                                    <img t-att-src="kanban_image('school.student', 'image', record.id.raw_value)"
                                         t-att-alt="record.name.value"
                                         width="80"/>
                                </div>
                                <div>
                                    <strong><field name="name"/></strong><br/>
                                    <field name="student_code"/><br/>
                                    <field name="class_id"/><br/>
                                    <span t-if="record.status.raw_value == 'studying'" class="text-success">Dang hoc</span>
                                    <span t-if="record.status.raw_value == 'dropped'" class="text-danger">Nghi hoc</span>
                                    <span t-if="record.status.raw_value == 'graduated'" class="text-info">Tot nghiep</span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>
